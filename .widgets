#!/usr/bin/env bash

_launch_tmux_session() {
    BUFFER=""
    CURSOR=0
    zle reset-prompt

    BUFFER="o"
    zle accept-line
}

zle -N _launch_tmux_session
bindkey "^o" _launch_tmux_session

bindkey -s "^f" "yazi\n"

bindkey -s "^g" "lazygit\n"
